import 'package:flutter/material.dart';

class GeneralConsultation extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("General Consultation"),
      ),
      body: Center(
        child: Text('This is the page for General Consultation'),
      ),
    );
  }
}
